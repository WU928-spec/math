# G1-razor（razor 带 (P) 第二证明的最后引理）— a1_

> 陈述：razor 角落 + 可装箱 ⟹ ∃ 可行装箱使 SS={s₁,s₂}（值序最小 2a 极端配对），
> 且被挤出 senior 的 SJ/S 安置可行。代码 code/a1_g1_razor.py。

## 1. 归约（已证件拼装）
G1-razor = [S1 已证：最小 2a 可极端配对（支配）] + **[挤出安置]（§21.5a 登记缺口）**。
挤出安置：真实装箱的 SS 集 Λ 换成 {s₁..s₂a} 后，Λ∖{s₁..s₂a}（挤出 senior e，
各与旧伴 e′ 满足 e+e′≤1）须入 SJ 槽；{s₁..s₂a}∖Λ（晋升 senior s′）腾出 SJ 槽
（槽 junior j′ 满足 s′+j′≤1）。c=0（razor）⟹ 无 S 槽，全部挤出入 SJ。
**方向修正（宿疾纪律自查）**：挤出 e ∉ {s₁..s₂a} ⟹ **e ≥ s_{2a+1} ≥ s′（晋升）**——
e ≥ s′ 恒成立（非 e ≤ s′）。故 e 入槽需 j′ ≤ 1−e ≤ 1−s′：槽的 junior 可能过大，
**挤出安置 = 更紧帽下的 junior 重匹配**（SJ-REV 全自由度 Hall 问题），不是同槽直换。

## 2. 证明骨架（razor 值带版，可形式化）
**δ-带 + 重匹配形式**：senior 带宽 δ ⟹ 挤出 e ≤ s′ + δ（e≥s′ 端差有界）。安置 =
junior 池上的链图匹配（Hall：M_new,(q)+u_(q)≤1，§21.1 已证形式）。M_new 比 M_old
的 senior 端**更难**（换入 ≥s_{2a+1}、换出 ≤s_{2a}）⟹ 真实匹配不蕴含规范匹配
（§3 补集反向支配：T≥Y0, U_J≥YJJ ⟹ U_S≤Y 逐分量——真实 SJ 用更小 junior，更易）。
**缺口精确形态**：规范 SJ junior 池 Y = 池∖(Y0∪YJJ) 是"最大剩余"，其链图 Hall
需 senior 端帽 e≤1−y_(q)；razor 挤压须给出 M_new 与 Y 的相容（定量簿记）。
修复候选：(i) razor 负载钉住 SS 箱亏空使重匹配有 ≥δ 净 slack；(ii) 证 hi 区挤出件
e ≤ 2t（SS 配对可行性给的上界！e≤2t 恒真）与 Y 的小端（y_(q)≤1−2t）相容——
**e ≤ 2t ⟹ 1−e ≥ 1−2t ≥ y_(q) 对 y_(q)≤1−2t 成立 ⟹ 小 junior 带内全相容**；
不匹配仅当 y_(q) ∈ (1−2t, 1−e]（中 junior 段）——回到 mid 宿主带分析。
**razor 值带证据**：§18.3 低端区 senior 全等（δ_low=O(挤压/MG)）✓；§21.5b 高端区
非恒全等 ✗——挤出 senior 若含高端件，δ-带形式需高端条件（登记）。

## 3. 数值判决（39 个 sjrev 承重点，code/a1_g1_razor.py）
- 见证提取（build_k 去 SJrev，t 网格）：37/39 成功；**m=7 (2,2,0,1,1,0) k=3 与
  m=8 (2,3,0,1,1,0) k=3 在 t 网格无见证**（供 main 查，或需更密 t 网格）。
- **装箱检验：0/37 可装箱**（typed mask DP）——sjrev 点同为幻影，与 razor 带
  (P) 一致；**G1 反例 0 个**（无可装前提，检验空真）。
- 结论：sjrev 点既不能确认也不能证伪 G1（前提空）；证成只能靠 §2 值带证明。

## 4. 分工与移交
- 我（w.l.o.g./计数）：归约 §1、δ-带证明骨架 §2（含缺口精确形态）、数值判决 §3。
- 缺口(i)/(ii) 与 §21.5b 高端条件属 agent-3/main 的 razor 动力学/挤压簿记域；
  若缺口(i) 得证（SS 箱亏空≥2δ），G1-razor 即闭合，razor 带 (P) 第二证明全域完成。
