# 敌意复核：a3_beta_hi.md（β v2.1 §5 补全终稿）— a1_

## 总判：**VALID（§5.1/§5.2 主体严密）；③ 情形 C1 的 s₂ 钉死论证不成立（如写）——但本案有统一
救法（τ<2t ⟹ 机 0 空窗），salvage 后 C1 闭合，β 收官有效。**

## ① §5.1（k=1 代数杀）：**VALID**
逐环核：q₁<4−9s₀（自 s₁=1−s₀>K−q₁=(5s₀+q₁)/4，代数 ✓）；fs: k=1⟹jj=0⟹机 0 取 q₁，
pair 需 q₁≥p−s₀+MG ✓；联立 p+MG<4−8s₀ ✓；C3(2s₀≥p+MG)⟹8s₀≥4p+4MG⟹4−8s₀≤4−4p−4MG，
传递 **p+MG<4−4p−4MG ⟹ p<4/5−MG**（MG 严格性逐环保持 ✓）；danger p>5/4−t≥11/12>4/5 ✗。
边界：q₁<4−9s₀ 在 s₀>4/9 时右端为负=即死，无害。s₀≥1−2t 未用（无害）。
反例搜索：每步 ≤/≥ 方向核验，无松动点。**k=1 死 VALID**。

## ② §5.2 三情形覆盖性：**VALID（两小注，无缺口）**
- 机 0 候选穷尽性：j₁ 不够格（H′类1 ✓）、t 不够格（t+s₀≤t+1/2≤5/6<p，用 s₀≤1/2 ✓）、
  互补件 k=2 全不高档（R∩low=∅ ✓；R∩hi 由 nofit 封死=H′类3，我的复核 VALID ✓）
  ⟹ 唯一候选 j₂ ✓ 真穷尽。
- 划分穷举性：q₁=max(j₂,1−s₂) 三态：j₂>1−s₂（A）、j₂<1−s₂（B/B′ 按高档）、
  等值 j₂=1−s₂：**未显式命名**，但等值时 j₂=q₁ 被 fs 钉走 ⟹ 机 0 空窗（同 A 之死）或
  j₂ 不高档（同 B′ 之死）——**结果无缺口**（建议在文中一句点名等值归属）。
- 情形 B 推导：nofit 4s₂>5s₀+q₁, q₁=1−s₂ ⟺ 5s₂>5s₀+1 ⟺ **s₂>s₀+1/5** ✓；
  q₁=1−s₂<4/5−s₀ ✓；j₂ 高档 p−s₀+MG≤j₂≤q₁<4/5−s₀ ⟹ p+MG<4/5 ✗ danger ✓ 严密。

## ③ 情形 C：**C1 如写 SUSPECT，salvage 后 VALID；C2 VALID**
- **C1（j₂=s₀ 钉）**：s₀=1−2t 双向夹 ✓ 严密；s₁=2t ✓；**但 "1−s₂=s₀ ⟹ s₂=2t=s₁" 无据**
  ——q₁=max(j₂,1−s₂)=s₀ 只给 **1−s₂≤s₀ ⟹ s₂≥2t=s₁**（可严格大于）。反例值组：
  t=0.28, s₀=0.44, s₁=0.56, s₂=0.70>s₁：K−q₁=3s₀/2=0.66，机 1 low(0.56≤0.66) 且
  机 2 hi(0.70>0.66)——**"同值相斥"失效**（其前提 s₂=s₁ 是 C2 的钉，C1 无）。草稿的
  "s₀=2/5 边界落 k≥3" 论证同样预设 s₂=s₁，不覆盖 s₂>s₁。
- **Salvage（核验通过，替代杀法统一且更简）**：C1 钉下 s₀=1−2t，p>5/4−t：
  **τ=1−p+s₀−MG < 1−(5/4−t)+(1−2t) = 3/4−t−MG < 2t**（t>1/4 窗口内 3/4−t≤2t，
  MG 给严格）且 **s₂≥2t**（上）⟹ **s₂>τ ⟹ 互补件 1−s₂ < p−s₀+MG 不够格**；
  又 j₂=s₀=q₁ 被 fs 钉在机 1 ⟹ **机 0 无候选、空窗死**。与 s₂ 是否等于 s₁ 无关，
  边界 t=0.3 亦覆盖（3/4−t−MG=0.45−MG < 0.6=2t ✓）。**C1 闭合**。
- **C2（s₂=s₁ 钉）**：s₀=1−2t 同夹 ✓；s₁=s₂=2t 同值，机 1 low ⟺ 2t≤3s₀/2 ⟺ t≤0.3、
  机 2 hi ⟺ t>0.3——同值过同一阈值 ⟹ 相斥 ✓（前提即钉，论证有效）；t≤0.3 时机 2 非
  hi ⟹ k 降级由 §3 接收 ✓。VALID。

## 数据背书一致性
k=1 7 例 / k=2 44 例全 B′ / case C 定向 1202 例 0 有效 / 15438 例 I<k——与证明链
（k=1 代数杀、B′=机 0 空窗主死因、C 空、链杀 I<k）一致；脚本可复跑（code/a3_hi_analyze.py、
a3_tight_stress.py），抽查口径无冲突。

## 结论
§5.1 VALID；§5.2 VALID（等值归属一句建议补）；§5.2-C：**C1 需换 salvage 杀法
（τ<2t ⟹ 1−s₂ 不够格 ⟹ 机 0 空窗，已核验，统一覆盖 s₂>s₁ 与 t=0.3 边界），C2 VALID**。
β v2.1 证明链在 C1-salvage 后闭合。建议 a3 将 §5.2-C1 的"相斥"段替换为 τ-杀法。
