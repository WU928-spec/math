# D 块：值语言序动态和界 f(r) —— 验证报告（a3_，收窄版：止步于验证）

> 2026-09-22，agent-3。任务（main 23:40 收窄指令）：找 f(r)（"值序最小 r 个 junior 的
> senior 伙伴之和 ≥ f(r)"候选界）+ 敌意核查界成立 + 登记 f(r) 表。**不做与 B 的整合/消费**。
> 工具：`code/a3_D_valsum.py`（角落 LP 无装箱+保序顶点采样，全可复跑）。

## 1. f(r) 实测表（角落 LP 可行域顶点，m=6/8/12 全 k 全 t 窗，随机目标采样，736 点）

值序最小 r 个 junior 的 senior 伙伴**均值**（=和/r）的最小值：

| m | r=1..nS−2 | r=nS−1 | r=nS |
|---|---|---|---|
| 6 | 0.458（平坦） | — | 0.462 |
| 8 | 0.458（平坦） | — | 0.472 |
| 12 | 0.458（平坦） | 0.468 | 0.477 |

平坦值 0.458 ≈ 11/24 = (5/4−1/3)/2 = **p/2 在窗口顶的值**。

## 2. 敌意核查判决：LP 松弛内无增强（界贴现有静态下界）

- 实测下界 **f(r) = r·p/2**，恰为现有静态下界：seniors 全 ≥ p/2
  （pair s_i ≥ p−j_i ≥ p−q₁ 与 q₁ ≤ a_m ≤ s_i ⟹ s_i ≥ p−a_m ≥ p−s_i ⟹ 2s_i ≥ p ⟹ s_i ≥ p/2；
  danger p > 5/4−t ⟹ s_i > 5/8−t/2，窗口顶 = 11/24 ≈ 0.458 ✓ 与实测吻合）。
- **含义**：角落 LP 可行域中，值序最小 r 个 junior 的 senior 伙伴之和的 LP 级下界
  = r·p/2，**贴静态下界**——juniors 值序与 seniors 在 LP 可行点上无耦合增强
  （razor/近 razor 点 seniors 近全等 ⟹ 任意 r 台均值 = 全等值 ≈ p/2）。
- 敌意核查（实测最小值 vs 候选更强界）：未找到 f(r) 超 r·p/2 的 LP 级证据；
  LP 可行域包含 juniors 值序与 seniors 解耦的点 ⟹ **任何更强的 f(r) 必引用 LP 区域外的
  序约束**（best-fit 填充序的完整内容，超出 LP 的保序投影）。

## 3. 与 B 的关系（收窄版仅登记，不整合）

D 块实测在 LP 松弛内：f(r) = r·p/2 无增强。B（值语言重构，agent-2）若给出 LP 区域外的
序约束（完整填充序的线性化），可能增强 f(r)——整合待 B 判决后另派。
本块结论：f(r) 表已登记（=r·p/2 贴边，实测 736 点 0 违反）；LP 内无增强证据；
界成立（=静态下界）但不足以供 (P)/(W′′) 闭环的链尾（JJJ 身份/值聚合需更强界）。

## 4. 诚实卡点

目标引理形态"值序最小 r 个 junior 的 senior 伙伴之和 ≥ f(r)（超静态）"在 LP 松弛内
被顶点实测证伪（贴 p/2）。这不是墙而是地图：LP 松弛含非动力学生成的点；
增强 f(r) 等价于把填充序的新内容 LP 化——与 §14（时序堵死）/§16（序确定性）
的接口在：填充序的**负载结果**（已填机负载结构）而非序位。本段未找到可 LP 化的增强。
