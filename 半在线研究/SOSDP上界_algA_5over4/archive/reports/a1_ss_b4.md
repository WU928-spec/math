# 引理 SS-B4 攻坚 — a1_

> 目标：razor 角落+可装箱+B4_q 破 ⟹ L_q 成员进 SS 不可行（g=0）。逃逸格（e=0）：
> (r,g)∈{(q,2),(q+1,1),(q+1,2)}（r=#{j≤θ}, θ=1−s_{nS+1−q}=:1−s）。

## 1. main §3 供给上界：独立复核 **VALID**（我的推导）
C junior j≤θ 的机器 senior 须 ≥ p−j ≥ p−θ =: **τ_q = p−1+s**；senior < τ_q 的机器
只能收 j > θ（非 C）。⟹ r 件 C junior 需 r 台 distinct senior ≥ τ_q 的机器：
> **r ≤ #{s_i ≥ τ_q}**（供给上界，方向复核 ✓）。
τ_q = p−1+s > s（p>1 段）⟹ #{s≥τ_q} = #{L_q ≥ τ_q} + #{D∩[τ_q, 1−s]}（D_w 窗）。
**D_w 可行性**：非空 ⟺ τ_q ≤ 1−s ⟺ s ≤ 1−p/2；宽度 2−p−2s。
(r=q+1) 格需 #{s≥τ_q} ≥ q+1 ⟹ **D_w ≥ q+1 − #{L_q≥τ_q}**（D-窗补足义务）。

## 2. 三格的簿记状态（值级计数穷竭后的存活态）
- **(q+1, 1)**：SS={s_L, d}，d∈D_w；d 机器 junior ≥ p−d ≥ τ_q（高位，非 C）✓ 自洽；
  L_q-SJ (q−1 台) 吃 C ∖{t,j₁,j₂}（恰 q−1 件）——**取等，C 耗尽**。
- **(q, 2)**：SS={s_a,s_b}⊂L_q 互配（和≤1，均∈(1−2t,1/2)）；L_q-SJ (q−2) 吃 j₃..j_{q+1}
  全部——**取等，C 耗尽**。
- **(q+1, 2)**：混合，同型。
**值级穷竭结论**：三格全部满足供给上界（D_w 补足）与 SJ 可行性（伙伴 ≤θ ≤1−s_L）——
**纯计数无矛盾**（与 main 9402 逃逸扫描 0 可装不冲突：值级可行 ≠ 角落+可装）。

## 3. 候选杀法判决
- **① L_q 互配对值带冲突**：两 senior∈(1−2t,1/2) 与其机器 junior ≥ p−s>p−1/2——
  单机器无矛盾（p−1/2 ≤ 2t 恒真）；需计数组合（见 ④）。
- **② D 伴侣高位机器 junior 与 C 小 junior 争夺**：d 机器 junior ≥τ_q（非 C）与 C 无争；
  真正争夺在"机器 senior ≥τ_q 的槽位"——已化为供给上界（§1）✓ 非独立杀法。
- **③ razor pinning + nofit/A5（正解候选）**：取等格 C 耗尽 ⟹ j₃..j_{q+1} 的分配唯一
  ⟹ L_q-SJ 机的机器 junior 被钉在 {j₃..j_{q+1}}（搭配 pair ℓ≥p 与 nofit/A5 的 k-结构）
  ——即 β v2 机制在 B4 场景的复用。**卡点**：B4 场景无 sliver 紧性，pinning 缺
  "每个 bin 全满"的前提——须用供给上界的**取等**（D_w 义务 + C 耗尽）替代紧性，
  其充分性 = 新引理（razor 取等供给 ⟹ mon2/nofit 杀），形式同 H′ 但输入为 B4 破。

## 4. 交付判决
- **无反例**（值级三格全部自洽，无可装见证——与实验 0/9402 一致）。
- **供给上界独立验证 + 锐化计数**（§1-2，可直接入主文档）。
- **精确卡点**：③ 的 pinning 杀在 B4 场景缺紧性前提；取等（C 耗尽+D_w 义务）是
  替代前提，其导出 mon2/nofit 矛盾的充分条件 = 新簿记引理（razor 取等供给引理），
  形式同 β-H′，输入换 B4 破。**建议**：main 将 §1 供给上界（已复核 VALID）与
  §2 取等结构入主，③ 按 β-H′ 模板执行（H′ 的 nofit 机制在 B4 场景的移植即
  引理 SS-B4 的最后一步）。
