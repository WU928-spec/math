# 口袋1 手证引理（P2K1/P2K-top 移植）— a1_

> 状态：**P1K1 已全 m 闭合**（待 agent-3 敌意复核）；P1K-top §2 骨架待逐环。
> 口袋1 角落原料（全部已审，LP_CONSTRAINTS.md §6/§7）：
> mach `s_i+j_i ≥ x+y`（M₀ 最闲）；danger `x+y > 5/4−t`；`x≤y`, `x≥t`, `y>1−2t`（大任务）；
> senior `s_i>1−2t` 升序；junior `j_i∈[t,2t)`；`j_i≤q₁`, `t≤q₁`, `q₁≤a_m`；
> `L=a_m+q₁≤1`；`K=(5/4)(a_m+q₁)`；装箱计数 `2a+b+c=m`, `b+3d+2e+f=m+1`
> ⟹ `a=d+e+f≥1`, `d=1+c+f≥1`（JJJ 恒存在）。
> **口袋1 特有**：`a_m = min(y, s_0)`（初始 m 件 = {y}∪{senior}，x 是后续件；LP 缺 `am<=y` 是
> 松弛缺口——手证按 dichotomy 显式处理）。t 窗口 `t∈((m−1)/(4(m−2)), 1/3]`。

## 1. 引理 P1K1（k=1，全 m≥4，全装箱，全 t 窗口）✅

**k=1 角落不存在。**

### 1.1 公共链（k=1 ⟹ jj=0，nofit 对全部 i≥1）
- nofit：`s_i + q₁ > K` ∀i≥1 ⟹ `s_i > (5/4)a_m + q₁/4`（用 `a_m≥q₁` 得 `>(3/2)q₁`）。…(N)
- mach + `j_i≤q₁`：`s_u ≥ x+y−q₁` ∀u。…(M)
- danger：`x+y > 5/4−t`，及 `x≤y` ⟹ `y > 5/8−t/2`。…(D)
- BB 箱（`a≥1`）：两 bigs 和 ≤1。大任务集 = seniors + y。

### 1.2 BB 箱为 senior-senior 对（含 a≥2 时的非 y 箱）
对 `{s_u,s_v}`（至少一个指标 ≥1，N 适用）：`1 ≥ s_u+s_v > (x+y−q₁)+(3/2)q₁ = x+y+q₁/2 > 5/4−t/2` ⟹ `t>1/2 > 1/3` 矛盾。✓
（a≥2 时 y 只占一箱，至少一个 BB 为 senior-senior，归此。）

### 1.3 BB 箱含 y 且 a=1（口袋1 特有障碍）
BB=`{y,s_v}`：`y+s_v≤1` 与 (M) ⟹ `x+2y ≤ 1+q₁`；减 danger：`x+y>5/4−t`：
> **(Y) `y < q₁ + t − 1/4`。**

**分支 1（a_m = y，即 y ≤ s₀）**：
- v≥1：nofit(a_m=y) `s_v > (5/4)y+q₁/4`；`y+s_v≤1` ⟹ `(9/4)y < 1−q₁/4` 即 `y < 4/9 − q₁/9`
  （agent-3 敌意复核修正系数）；与 (D) 联立 `q₁ < 9t/2 − 13/8`，又 `q₁≥t` ⟹
  **`t > 13/28 > 1/3`**。✓
- v=0（BB=`{y,s₀}`）：(Y)+(D) 给 `q₁ > 7/8 − 3t/2`；其余 senior 归宿：
  - **3a 某 senior 入 BJ**：`s_i≤1−t` 与 nofit 联立 ⟹ `q₁ < 7/8 − 3t/2`，**与上式严格矛盾**（与 t 无关）。✓
  - **3b 其余 senior 全 B 独箱（b=0）**：见 §1.4 箱数链。✓

**分支 2（a_m = s₀，即 s₀ < y）**：（展示链经 agent-3 敌意复核以 Farkas 重构为准）
`mach0: s₀+j₀ ≥ x+y`、`j₀≤q₁`、`q₁≤s₀`、`s₀≤y` 四链 ⟹ `y+j₀ ≥ x+y` 即 `j₀ ≥ x`；
再与 nofit_v、BB、danger 组合（Farkas 支撑 7 行权比 5:5:5:4:4:1:4，m=12/14/18/24
逐系数相同）⟹ **`t > 1/2`**（证书版 **t > 9/20 > 1/3**）矛盾。✓
（旧稿"q₁>7/8−3t/4 + L≤1 ⟹ t>2/5"系笔误链，作废——sympy 旧验只验传递性未验前提，
agent-3 指正，已换。）

### 1.4 窄核闭合（箱数链，一般分组成立）
3b 配置：k=1 ∧ `a_m=y` ∧ `a=1` ∧ BB=`{y,s₀}` ∧ b=0（无 BJ，其余 senior 全独箱）。
bigs 占箱 = 1（BB）+ (m−2)（B 独箱）= **m−1 箱**，剩 **1 箱**装全部 smalls。
m+1 个小件各 ≥ t；窗口 t>1/4 ⟹ 4t>1 ⟹ **每箱至多 3 小件**。
1 箱至多 3 件 < m+1（∀m≥3）——**箱数矛盾**。✓

**符号链来源（Farkas 恒等式反推，佐证）**：mini-LP 的 m-无关恒等式中 JJJ0 行
（j_last+x+tv≤1——固定分组下 x,tv 被迫同箱）+ BB0 + mach0 + danger×2 + nofit1 + fit
+ j0 界，正是"smalls 被迫挤进剩余 1 箱"在固定分组 LP 上的投影；3a/3b 把它还原为
分组无关的纯组合链。恒等式（m=12/18/24 逐系数相同，列平衡逐变量核验过）：
`{tv>=t:59952, danger:119904, mach0>M0:59952, j0<=am:49960, j0<=q1:9992,
 j_last>=t:59952, sk+q1<=K:9992, nofit1:19984, BB0:79936, JJJ0:59952, y<=s0:59952}/10000`，
RHS 和 `= −9992 − 79936·MG < 0`（t 项自相消）。
（装箱固定分组仍 SUSPECT——恒等式仅佐证；§1.2/§1.3/§1.4 全部链为一般分组。）

**P1K1 证讫**：§1.2 ∪ 分支1-v≥1 ∪ 3a ∪ 3b ∪ 分支2 全覆盖。∎（待敌意复核）

## 2. 引理 P1K-top（k≥m−2，端口骨架）— 部分闭合

jj≥nS−2：低端区 0..jj（fit+srt ⟹ s_i≤K−q₁）、高端区 ≤1 台（nofit s>K−q₁）、
低端 junior 升序（保序推论）。SS 箱 {s_u,s_v}（a≥1, u<v）两支：

### 2.1 高端支（v≥jj+1，即 v=nS−1）✅ 移植直闭，a_m 无分歧
nofit `s_v > (5/4)a_m+q₁/4`；`s_u+s_v≤1` + mach `s_u≥x+y−q₁` ⟹
`x+y < 1−(5/4)a_m+(3/4)q₁`；danger ⟹ `(5/4)a_m < (3/4)q₁+t−1/4`；
`a_m≥q₁` ⟹ `q₁ < 2t−1/2` ⟹ `t>1/2`（q₁≥t），与 t≤1/3 矛盾。✓
（a_m≥q₁ 两分支皆成立，无需 dichotomy。）

### 2.2 低端支（v≤jj）mach+保序 部分
mach×2：`j_u+j_v > 3/2−2t`；保序 `j_u≤j_v` ⟹ `j_v > 3/4−t` ⟹ `q₁ > 3/4−t`
⟹ `a_m < 1/4+t`。
- **a_m=s₀ 支** ✅（agent-3 终审补 x 计数后仍成立，分两种情形）：
  `j_0 > 5/4−t−s₀ = 5/4−t−a_m > 1−2t`（用 `a_m < 1/4+t`，来自 `q₁>3/4−t`+L≤1），
  低端 junior 全 >1−2t。JJJ 池（口袋1 比口袋2 多 x）= {t, x, 高端 junior}：
  - **x > 1−2t**：池 = {t}∪{高端 junior} ≤ 1+(m−2−jj) ≤ 2 件（jj≥nS−2）<3 ⟹ d=0 矛盾。✓
  - **x ≤ 1−2t**（agent-3 指出的漏数情形）：池恰 3 件可成箱 JJJ={t,x,j_h}，
    `t+x+j_h≤1` ⟹ `j_h ≤ 1−2t`。且 **d=1 被强制**（第二个 JJJ 需 3 个 junior 各 >1−2t，
    和 >3(1−2t) ≥ 1（t≤1/3）矛盾）⟹ c=f=0，结构刚性化：a=1+e, b=m−2−2e。
    该刚性壳的纯符号杀手未最后落定；**mini-LP 兜底**：k=m−2+保序+x≤1−2t 全 cnt
    常数证书 INF（m=6/8/12 agent-3 证，agent-1 对拍复现 m=6/8/12 5/9/23 全过）。
    结论成立，缺口仅"该壳的望远镜恒等式"（与固定分组 SUSPECT 同源，登记待补）。
- **a_m=y 支** ✅ **已闭合（ord 链，经 certified 恒等式反推）**：
  上一段"见证点"实未含 ord（ claims 有误，已修正）。**含 ord（h=0 强制）后 a_m=y 全域
  INFEASIBLE**（m=14/18 证书，m=14 支撑 10 行）。可读链（(1,m−2,0,1,0,0) 族）：
  1. mach0+mach1：`s₀+s₁ ≥ 2(x+y)−(j₀+j₁)`；BB0：`s₀+s₁≤1`；fs_j0/fs_j1：`j₀,j₁≤j_jj=q₁`
     ⟹ `q₁ ≥ j_jj ≥ x+y−1/2`。（一般地：任两 BB senior 的 mach 和 + j≤q₁ ⟹ `q₁>3/4−t`。）
  2. JJJ0（本族固定分组：j_jj+x+tv 同箱）：`q₁+x+tv ≤ 1`。
  3. 1+2：`x+y−1/2+x+tv ≤ 1` ⟹ `x+tv < 1/4+t`（danger 移项）；`x≥t, tv≥t` ⟹
     **`t ≤ x < 1/4`，与窗口 t>1/4 矛盾**。✓（sympy 核过：3/4−t<1−2t ⟺ t<1/4）
  m=18 族 (3,12,0,1,2,0) 有第二条 ord 重恒等式（ord2..ord13 + mach2/3 + BB1 + j13≥t +
  JJJ0，RHS −1/2503 类），同样 m-无关。
  **诚实标注**：两恒等式含固定分组内容（JJJ0=j_jj+x+tv / BB 配对），分组无关的一般
  cnt 链开放（候选结构引理："q₁ 被迫与 x,tv 同箱"或逐族恒等式族）。计算管线已逐 cnt
  闭合 m≤26，故缺口仅为"符号统一性"。

## 3. 自查记录
- sympy 代数核对：§1 全部不等式链（a1_p1k1_sympy.py）；箱数链纯计数。
- mini-LP：P1K1 窄核 m=12/14/18/24 全 INF + m-无关恒等式（佐证）；P1K-top 高端支
  不需；低端 a_m=y 支见证提取（§2.2）。
- 消融（每条链承重原料）：随敌意复核补。
