# 敌意复核：main_beta_proof_draft.md（β 引理证明第一稿）— a1_

## 总判：**SUSPECT（一处实质缺口 + k=2 节缺 nofit 后果），结论可经修复保，但机制非如稿所写**

## ① 引理 H（高位 junior 存货 ≤2）：**SUSPECT——"至多 1 件"无证明，且纯值界下为假**
**反例**（满足稿中除 nofit 外全部条件）：t=1/3, s₁=s₂=0.5(C2 紧), s₃=0.51, s₄=0.52,
p=0.9167：τ=1−p+s₁=0.5833，s₃,s₄ ≤ τ 同时成立 ⟹ 互补件 1−s₃=0.49, 1−s₄=0.48 与
j₂(≥p−s₁=0.4167, 取 0.42) **三件 ≥ p−s₁**——存货=3，k=3 复活。稿的"归入 k 链更强
约束"无实质（k 链正是被界定对象，循环）。
**修复（nofit，核验通过）**：k=jj+1 首步要求机器 k+1.. 放不下 q₁：s₃+q₁>K=5(s₁+q₁)/4
⟹ s₃>(5s₁+q₁)/4；而 (5s₁+q₁)/4 ≥ τ ⟺ s₁+q₁ ≥ 4−4p（**恒真**：s₁+q₁ ≥ p > 5/4−t > 4t−1
≥ 4−4p，t<9/20 窗口内恒）。⟹ **nofit 强制 s₃ > τ ⟹ s₃,s₄,… 全部不够格 ⟹ 互补存货=0**。
修复后引理 H 成立且更强（存货 ≤ 0+1+0 = 1，仅 j₂ 可能够格）。
**连锁影响**：稿 k=2 节"若取 1−s₃（s₃≤τ）"子情形被 nofit 杀死（s₃≤τ ⟹ s₃ 收 q₁ 破
首步）；k=2 仅剩 x₁=j₂, q₁=1−s₃ 形，且 nofit 附加 **s₃ > s₁+1/5**（由 s₃+1−s₃=1 > K
化简）——稿未写此约束。k≤2 结论在修复下幸存（机制 = nofit 排除互补存货，非稿写的
"s₃,s₄ 不会同时够格"）。

## ② C2（s₁∉SS 排除）：**VALID**
v=1−u ≤ 1−s₂ ≤ 1−s₁ ≤ s₁（s₁≥1/2 由 q₁≥1−s₁∧q₁≤s₁）且 v ≥ u ≥ s₂ ≥ s₁、v≠s₁
⟹ v=s₁ 矛盾。逐环严密 ✓。v=s₂ 分支的 q₁≥1−s₃ 替代亦正。

## ③ k=2 fs/lowzone：**已写部分逐核正确，但缺 nofit 后果（不完全，非错误）**
- s₂=1−s₁（C2）✓；q₁=max(j₂,1−s₃)，x₁=j₂ 时 q₁=1−s₃ 需 1−s₃≤s₁ ⟺ s₃≥s₂ 恒真 ✓；
- 机 2 pair：s₃≤s₂=1−s₁ ⟹ s₂+q₁=2−s₁−s₃ ≥ p+MG ⟸ 0≤1−p−MG 恒真 ✓；
- lowzone：q₁ ≥ 4−9s₁（恒 <0，s₁≥(5/4−t)/2>4/9）✓ 自动满足。
- **缺**：q₁=1−s₃ 形下 nofit 给 s₃>s₁+1/5（收紧 hi 区输入，稿未含）；x₁=1−s₃ 形已死（①）。
- hi 区定性判断（大 senior pair 下界低但 cap K−sᵢ 小 ⟹ 只收小 junior）方向合理，
  与修复后 τ 机制一致 ✓，但定量义务仍在（稿自承唯一缺口，合理）。

## sliver 紧性引理：第三证（体积账，e=0/1 同式）
量守恒：全体积 = p-bin + (t+J+u+v)[JJJ+SS] + SJvol + JJvol = p+t+Σℓ。
SJvol ≤ b = nS−2；JJvol ≤ e ≤ 1；Σℓ ≥ nS·p（pair 下界）。
⟹ t+J+u+v ≥ p+t+nS·p−(nS−2)−1 = p+t+nS·p−nS+1 ≥ 2
⟺ p(1+nS) ≥ nS+1−t ⟸ (5/4−t)(nS+1) ≥ nS+1−t ⟺ (nS+1)/4 ≥ t·nS ⟺ t ≤ 1/4+1/(4nS)
（窗口 t≤1/3、nS≥2 恒真）✓。又 t+J+u+v ≤ 2（两箱）⟹ **=2（全满）**；
sliver（jᵢ>1−J ∀i≥3）+ 全满 ⟹ JJJ 恰 (t,j₁,j₂) 且每 SJ 箱 s=1−j（互补）✓。∎

## 结论与建议
- 稿的 §3 骨架（C2+存货+升链 ⟹ k≤2）**方向正确、结论可保**，但引理 H 须改写为
  **"nofit 下互补存货=0"**（当前文本的反例间隙正是任务点①）；k=2 节需补 nofit
  约束（s₃>s₁+1/5）并删除已死的 1−s₃ 子情形。
- §3 hi 区缺口判定合理（唯一缺口，交 agent-3 对口）。
- 建议 main 吸收修复后 SEMANTICS 登记："β 证明稿 = SUSPECT→修复后 VALID 骨架
  （引理 H 改为 nofit 版；k=2 补 nofit）"。
