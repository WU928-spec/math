# 敌意复核：main_beta_proof_v2.md（nofit 版 β 引理）— a1_

## 总判：**VALID 骨架，③ 有一处方向错误（结论可经 C1 正法救回，证法须改）**；①②④ 全过。

## ① 引理 H′（Q ≤ k−1）：**VALID**（三子项全严密）
(a) **划界正确**：0-indexed，SS={s₀,s₁}，R={s₂..}；low=机器 0..jj（k=jj+1 台）；
  R∩low = {s₂..s_{jj}} = k−2 台 ✓（s_{jj}∈R：非 SS 成员 ✓）。
(b) **hi 永不够格链严密**：nofit s>K−q₁=(5s₀+q₁)/4；(5s₀+q₁)/4 ≥ τ ⟸ s₀+q₁ ≥ 4−4p
  （−4MG 额外 slack 自动吸收）；s₀+q₁ ≥ 1−t（band s₀≥1−2t ∧ q₁≥t）且
  1−t ≥ 4−4p ⟺ p ≥ (3+t)/4 ⟸ p>5/4−t（t≤2/5，razor t≤1/3 ✓）——逐环 ✓。
  s > K−q₁ ≥ τ ⟹ 1−s < p−s₀+MG ⟹ 不够格 ✓（严格性与 τ 的 −MG 对齐 ✓）。
(c) **j₁/j₂ 边界正确**：j₁ ≤ (1−t)/2 < p−s₀（链：p−(1−t)/2 > 3/4−t/2 > 1/2 ≥ s₀，
  三环全过）✓；j₂ 够格 ⟺ 2s₀≥p（单件）✓。
计数：互补 low ≤ k−2 + j₂ ≤ 1 + j₁ 0 + hi 0 = **k−1** ✓。

## ② mon2 升链杀（k≥3）：**VALID**
x₀ ≥ p−s₀+MG ∧ 链不减 ⟹ k 件（distinct 机件）全 ≥ p−s₀+MG ⟹ Q ≥ k > k−1 ≥ Q 矛盾 ✓。
（任务提示的"后件只需 ≥p−sᵢ 更弱"不影响：链给出更强的一致下界，正是 Q 所需 ✓。）

## ③ C2 的 q₁=s₀ 钉：**结论 VALID，但所写推导有方向错误（宿疾 #11）**
v2 原文："q₁≥1−s_min_R≥1−s₁ 且 q₁≤s₀ ⟹ s₁≥1−s₀"。**方向错**：s_min_R = s₂（R 从
s₂ 起）且 s₂ ≥ s₁ ⟹ **1−s₂ ≤ 1−s₁**——"1−s_min_R ≥ 1−s₁" 反向（等号仅当 s₂=s₁）。
反例 s₁=0.48, s₂=0.53, s₀=0.52：1−s₂=0.47 < s₀，j₂=0.50 < s₀ ⟹ q₁=0.50 < s₀——
v2 的"q₁=s₀ 恒钉"按所写链不成立（且此时 s₀+s₁=1.00 恰成立但 v=s₂ 情形使
q₁=max(j₂,1−s₁)=s₀ 又另论——case 依赖 v 的身份，v2 未分）。
**正法（可救回结论）**：C1（max J = q₁ ≤ s₀）⟹ 每个 1−s（s∈R）≤ s₀ ⟹ **全体
R senior ≥ 1−s₀**；又 v = 1−s₀ 是 SS 伴（紧性）且 v ≥ s₁：s₁ ≥ 1−s₀ = v ≥ s₁
⟹ **v = s₁ ∧ s₀+s₁ = 1** ✓；此时 q₁ = max(j₂, 1−s₂) 且 1−s₂ ≤ 1−s₁ = s₀、
j₂ ≤ s₀：q₁ ≤ s₀——**q₁ = s₀ 需 j₂=s₀ 或 s₂=s₁**，非恒真！v2 的"q₁ = max(j₂,1−s₁)
= s₀" 同样错（1−s_min_R = 1−s₂ 非 1−s₁）。**修正后结论**：C2 的 SS={s₀,s₁} 紧成立；
**q₁ ≤ s₀（C1），q₁ = s₀ 仅当 j₂=s₀ ∨ s₂=s₁**——q₁=s₀ 是条件性钉，非恒钉。
下游影响：C3（x₀ ≤ q₁）弱化（x₀ ≤ q₁ ≤ s₀：机 0 pair 需 p−s₀+MG ≤ q₁，即
**p−s₀+MG ≤ q₁** 新窗口条件，较 v2 的 p≤2s₀−MG 弱——k=2 节的 j₂ 条件相应改为
p−s₀+MG ≤ j₂ ≤ q₁）。§3 升链杀不受影响（用 p−s₀ 下界 + Q 计数，与 q₁ 钉无关）✓。
**判 SUSPECT（证法），结论降级为条件性**。

## ④ k=2 化归 §4：**基本 VALID，一处过严（无害）**
- R∩low = ∅（jj=1）✓ ⟹ x₀ 只能 j₂ ✓。
- "j₂ < s₀ 严格"**过严**：j₂ = s₀（值）可行（j₂ 与 q₁ 是 distinct 件，同值无碍；
  机 0 pair 2s₀ ≥ p+MG 即 C3 ✓）。实为 j₂ ≤ q₁ 且 distinct 自动满足 ✓。
- 窗口条件 p−s₀+MG ≤ j₂（≤q₁）：j₂ = 1−t−j₁（紧性）代入 ⟺ p−s₀ ≤ 1−t−j₁；
  不满足 ⟹ 机 0 空死 ✓。承接性 OK（q₁ 钉降级后上界改 q₁，§4 仍成立）。
- §5 hi 区缺口自承 ✓（机制方向与修复后 τ 一致）。

## 结论
v2 四环：**①② VALID，③ SUSPECT（方向错误，结论经 C1 正法部分救回：SS={s₀,s₁} 紧
VALID；q₁=s₀ 降级为条件钉 j₂=s₀∨s₂=s₁；C3/§4 相应弱化但成立），④ 基本 VALID**。
§3 主杀（k≥3 死）不受影响——β 引理骨架在修正 ③ 后仍闭合（k=2 与 hi 区义务随 q₁
钉弱化稍移，§5 待 agent-3 时以上界 q₁ 表述）。
建议 main 修正 C2 证法（改用 C1⟹R≥1−s₀ 链），并在 §4 以 q₁（≤s₀）代 s₀ 重写窗口。
