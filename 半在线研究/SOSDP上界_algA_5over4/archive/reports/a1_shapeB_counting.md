# 形 B (cnt=(2,m−5,0,1,1,0)) junior 预算矛盾 — a1_ **修订版（v2，敌意复核后）**

> 修订（2026-09-23，应主代理敌意复核）：v1 步骤 3 的 `1−2t ≤ τ` 在 t<1/3 为假
> （1−2t > τ ⟺ t<1/3）——大 senior 伴侣帽 1−s 可落 (τ, α]（α=1−2t），
> 不需 ≤τ，矛盾断链。**v1 的"纯计数 ∀m 闭合"判决撤回**。数值搜索
> （m=6..14 × 5t × 400 试，typed_feasible 精确验证）仍 0 成功，作证据保留。

## 1. 三阈值重算（修正版预算）
段界：τ=(1−t)/2（JJJ 资格：j≤τ），α=1−2t（大 senior 伴侣帽上界，s>2t ⟹ 伴≤1−s<α）。
窗口内恒有 τ<α（t<1/3）⟹ 三段：small≤τ | mid (τ,α] | large (α,2t)。
t<3/10 时 SS 机 junior 下界 5/4−3t 落在 **mid 段**（见数值表：t=0.26/0.28/0.30 均
(τ,α]）——**SS 机 junior 是 mid，可作大 senior 伴侣**（帽 α 容得下 mid）⟹ v1 的
"4 件 mid 被 pair 钉死"不成立：它们可流动到大 senior 的 SJ 箱。

## 2. 修正后的预算（紧平衡，非矛盾）
OPT junior 需求（nS=m−1 件，**恰好相等**）：
- JJJ：2 件 ≤τ（small）；
- 大 senior（m−5 台，s>2t）：各 1 伴 ≤α（small 或 mid 均可）；
- JJ：2 件（任意，mid+small 或双 small）。
- SS seniors 入 SS 箱不占 junior。
合计 2+(m−5)+2 = m−1 = nS：**计数取等，矛盾消失**。可行性取决于值分布穿针：
2 件 ≤τ 存在、伴侣在帽内配平、mid 各得其所——**这正是主代理"三元组-匹配交互"
的 razor 带形态**（计数层不闭合，须匹配/结构论证）。

## 3. 判决（诚实）
- **形 B 的 (P) 不能由纯计数闭合**（v1 主张撤回；复核正确）。
- 纯计数矛盾的唯一逃生门被堵条件：4 台 SS 机 junior 全 >α（不可作伴侣）⟺
  全 SS senior < p−α ≈ 1/4+t——**角落约束不强制**（SS senior 可至 2t > 1/4+t，
  t>1/4 恒真）。若未来某引理迫使 SS senior < 1/4+t（挤压引理只给负载和，
  不给单项），计数矛盾即复活——登记为条件性推论。
- 数值证据：m=6..14 构造搜索 0/10000+（含两模态），支持"可行实例极稀/可能没有"
  但未证；razor 近失（m=6 t=0.32 JJJ 差 0.01）显示紧平衡的真实性。
- **(P)-razor-B 回归结构论证路线**（匹配-三元组交互 / 区间定理端点），
  与主代理收官定位一致。

## 4. 对 v1 引理文本的处置
v1 引理（t<3/10 计数矛盾）标记 **INVALID（步骤 3 边界错误）**；
保留：三阈值分析、紧平衡预算、数值搜索记录、条件性推论（SS senior < 1/4+t
⟹ 矛盾复活）。SEMANTICS 登记建议：a1_shapeB_counting v1 = INVALID（复核撤回）。
