# E-nec-v3 裁决报告：+挤压行+值带行的 razor 带决战（agent-4，2026-09-23）

代码 `code/a4_enec3.py`（build_v3 = build_v2a（mon2+X 行，无固定身份装箱行）+ XSQZ + XVB）。

## 新入行与合法性出处（护栏：逐行引述）

| 行 | 内容 | 合法性出处 |
|---|---|---|
| XSQZ（1 条，全域） | Σᵢ(sᵢ+jᵢ)+t ≤ m−1 | razor 挤压引理（JEL.md：p 独箱+OPT 箱≤1 ⟹ Σℓ≤m−1−t），全域 cnt 无关 |
| XVB{i}（nS 条，仅 c=0） | sᵢ+t ≤ 1 ∀i | 值带结构①（JEL.md：senior>1−t 时 SS 和>2−3t>1、SJ 伴需<t≤任意 junior ⟹ 无处可去）；c≥1 时 senior 可独箱故不合法，本版只在 c==0 加入 |
| XPS（伴侣供给计数） | **未实现** | 需 junior 全局序或逐 senior 析取，单条线性行不可表达（登记） |

编码正确性核验：V2a 旧挤压超限点（Σℓ=15.62>14.62）被 XSQZ 排除 ✓；
V3 幸存者点精确饱和新行（Σℓ=m−1−t、s_max=1−t 等号）✓——行有牙、无符号翻转。

## 裁决结果（决定性负面）

| 测试集 | V2a | V3（+挤压+值带） |
|---|---|---|
| razor 残留 465 处重闭合 | 0（定义） | **1/465** |
| 洞族 20 处 | 20/20 存活 | **20/20 存活** |
| m=4..14 总保持率 | 779/1244 = 62.62% | 780/1244 = 62.70%（+1） |

- 挤压行对洞族零杀伤与 razor 点数据一致（ghost 点 0/14 违反挤压预算——它们天生满足）；
- 消融：build_close（caps+mon2）洞闭合在加挤压后保持 INF ✓；去 mon2 留 caps+挤压 → FEAS
  （**挤压不能替代 mon2**）；V3 闭合的 c=0 抽样 6 例中去挤压/去值带均不回弹（保持区冗余陪跑）；
- 幸存者结构：V3 可行点把 Σℓ 与 s_max 精确压到新行边界（等号饱和）——多胞体**恰好有空间**
  滑到边界，聚合行不够紧。

## 结论

1. **挤压引理+值带结构作为 LP 行不足以闭合 razor 带**：它们的约束力是聚合式的，
   而 razor 带的承重内容是**箱内身份**（哪个 junior 进 JJJ/SJ、SS 配对身份）——
   与 E-nec-v2 判决一致并强化：(W'')/刚性证明义务**不可经 LP 合法行绕过**。
2. razor 带闭合的确切缺口 = 身份内容；可用原料底线：mon2（序）× 固定身份行（(W'')）联合。
3. 数据资产：`code/a4_razor_points.jsonl` 90 点结构 + 本报告闭合率；
   若后续要再试 LP 路线，唯一剩余候选 = junior 身份型合法行（需 hi 区 junior 序引理，
   a4_enec.md §4 登记）或 agent-2 计数流刻画的析取强化版（需角落内坍缩证明）。

## 诚实登记

- 全部为浮点 LP 判定（fast 管线/farkas_fixed.float_cert）；编码核验见上（饱和签名）。
- XPS 未实现（表达性缺口，已登记）；②③④ junior 值序强形仍缺席（同 v1/v2 登记）。
- "挤压+值带不足"的结论对本行族选择稳健；不排除更强的身份型合法行。
