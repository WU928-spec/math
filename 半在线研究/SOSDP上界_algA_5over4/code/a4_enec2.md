# E-nec-v2 裁决报告：mon2 + 必要条件行（替换固定分组装箱行）（agent-4，2026-09-23）

代码 `code/a4_enec2.py`（build_v2a = hole_close.build_close 骨架（build_fixed 全约束+mon2）
去 SS/SJ/JJJ/JJ caps + XSS/XVOL/XBIG 必要条件行，行族合法性论证见 a4_enec.md §0）。

## 0. 与 E-nec v1 的关系澄清

v1 retain 实测 = rows_fixed(use_order=True)（ord≡mon2 + lowzone/hizone）+ X 行，
**保序当时已在场**（BOARD 合成架构条中"62.6%保持已验"引用无误，但"在无保序基底上测"的
表述需更正：62.6% 就是含保序的组合数）。v2a（build_close 骨架）与 v1 保持率**逐项一致**
（779/1244，损失集完全相同）⟹ ①ord≡mon2 数值确认等价；②lowzone/hizone 边际强度为零。

## 1. 洞族命运（决定性负面）

**20/20 鬼影洞在 v2a 下存活**（m=12..20 全洞族；加 t≤1/3 显式窗口行后仍 20/20 存活——
鬼影点在 t=1/3 边界，窗口内）。对照：build_close（含 caps+mon2）同骨架全杀（hole_close 已证）。

**结论：固定分组装箱行对鬼影洞族是承重的**——mon2 单独不行（需 caps 联手），
{XSS,XVOL,XBIG} 必要条件行不能替代 caps。这与 E-nec v1（无 mon2 时 X 行 0/20）合并为：
**鬼影洞的闭合 = mon2 × 固定身份装箱行 的联合效应，二者缺一不可**。

## 2. 保持率与 razor 残留（v2a = v1 组合版，62.6%）

- m=4..14 全量 1244 情形：保持 779（62.6%），损失 465。
- 损失族分布（损失/族总数）：(0,1,e,0) 族随 e 减而愈重（e=0: 75/88 … e=5: 3/25）；
  (0,2..3,e,1)、(1,2..4,e,0/1/2)、(2,3..4,…) 各小族同型递减——**损失遍布 razor 及其邻近带，
  非 razor 深处（c≥2 或 d≥4 或 a≥3）基本保持**。
- razor 残留精确清单（(1,m−3,0,1,0,0)：k=2..m−1 全残留；(2,m−5,0,1,1,0)：k=4..m−1 残留）已落
  本报告与 BOARD；**但注意**：残留 LP 可行点抽样 t=0.375..0.413 全在 t>1/3（窗口外）——
  加 t≤1/3 行后仅 52/465 重闭合，**其余 413 处损失在窗口内真实可行**。
  ⟹ razor 带在"mon2+必要条件行"下的窗口内残留 = (W'')（或 razor 刚性）真正不可绕过的最小区域。

## 3. 消融（承重分配）

razor 带闭合代表（v2a INF 处）：去 mon2 → FEASIBLE（回弹）；去全部 X 行 → FEASIBLE（回弹）；
去单族 XSS/XVOL/XBIG 多数仍 INF（冗余）。**承重 = mon2 × X 行联合，单族 X 可互替**——
在 caps 缺席处，mon2 与计数行谁都单独不够。

## 4. 判决与建议

判决树走到 **razor 带残留**分支：
1. (P) 不经 (W'') 的 LP 路线在本行族下**不成立**——固定身份装箱行的身份信息
   （哪个 junior 进 JJJ/SJ）正是 razor 带+鬼影洞的承重件；
2. 主攻收窄建议（与合成架构三区图一致）：razor 带 (1,m−3,0,1,0,0)@k≥2 与 (2,m−5,0,1,1,0)@k≥4
   的窗口内残留 = (W'')/刚性证明的最小区域；非 razor 深处必要条件行已够；
3. 若要在 LP 内救回 razor 带：需要带 junior 身份的合法必要条件——最小候选 =
   **②的 junior 值序强形（最小 3d 聚合）+ hi 区 junior 序引理**（a4_enec.md §4 已登记），
   或 SJ 匹配 Hall 的合法阈值形（需先解决 SJ senior 身份不定）；
4. 诚实登记：X 行强度是实现相关的（XVOL/XBIG 弱形）；②③④ 未实现使本裁决是
   "必要条件路线"的下界估计；但鬼影 20/20 存活在 XSS（已比默认 SS 更强）在场下发生，
   该负面结论对 senior 侧行族选择稳健。

## 5. 验证纪律

全部 INFEASIBLE/FEASIBLE 结论经浮点 LP（farkas_fixed.float_cert）；洞族存活点
位于 t=1/3（窗口边界）；残留点提取经 resid_point（bt 并入 t 列的 linprog 零目标）复核；
两骨架（build_close / rows_fixed）对拍一致（779/1244 同集）。
